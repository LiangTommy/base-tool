package com.rt.tools.core.strategy;

public interface StringStrategyInterface {
    /**
     * handle the string
     * @param str sourceStr
     * @param limit character max limit condition
     * @return result
     */
    String handle(String str,int limit);
}
